spring:
  application:
    name: zapter-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/zapter
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
  security:
    jwt:
      secret-key: 3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
      expiration-time: 300000
    user:
      name: user
      password: pass
      roles: USER
  mail:
    host: smtp.gmail.com
    port: 587
    username: sohrabmastersolutions@gmail.com
    password: jkrbsisscnncrwmg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable:true

razorpay:
  key-id: rzp_test_SYcy5fobkxRpqE
  key-secret: TlonLy6dF6qIIB5LgLF2sLS3

# ─── Twilio Verify (no phone number purchase needed) ──────────────────────
# HOW TO GET verify-service-sid (one-time, 2 minutes):
#   1. Go to https://console.twilio.com
#   2. Left sidebar → Verify → Services → "Create new Service"
#   3. Name it "Zapter" → Create
#   4. Copy the Service SID (starts with VA...) and paste below
  # ← replace this